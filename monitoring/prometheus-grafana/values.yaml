grafana:
  enabled: true
  service:
    type: LoadBalancer

prometheus:
  prometheusSpec:
    retention: 5d
    resources:
      requests:
        cpu: "200m"
        memory: "256Mi"
      limits:
        cpu: "500m"
        memory: "512Mi"

alertmanager:
  enabled: false
