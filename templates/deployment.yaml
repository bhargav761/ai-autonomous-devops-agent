apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ai-dev-agent.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "ai-dev-agent.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "ai-dev-agent.name" . }}
    spec:
      containers:
        - name: ai-dev-agent
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
